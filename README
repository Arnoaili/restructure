# blue
 * 蓝图

# script
 * 功能模块文件(从ALM获取数据存入数据库)

# settings
 * conf文件

# templates:
 * 静态html文件

# static:
 * js css文件
 * 包括需要载入的js插件


# 代码优化

## 命令规范

  * 变量命名统一用驼峰法命令
  * 函数统一用小写的字符串，可以添加下划线增加可读性
  * 类使用驼峰格式命名，即所有单词首字母大写其余字母小写，不允许超过10个字符宽度的命名
  * 模块和包统一使用小写字母，不允许使用下划线
  * 常量用全部大写字母表示，可以用下划线增加可读性


## 注释

  * 所有注释都必须用英文书写
  * 不允许使用行内注释
  * 非公开方法(调用的函数)必须有注释

## doc_string

* 所有公共模块、函数、类和方法必须书写文档字符串
* 单行的文档字符串，结尾的 """ 在同一行
* 结尾的"""必须单独成行

# 其他的严格遵循pep8规范，以上是强制约束，仅对bug web项目
